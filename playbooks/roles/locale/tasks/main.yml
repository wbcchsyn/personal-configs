- include: debian_packages.yml
  when: ansible_os_family == 'Debian'

- include: debian_locales.yml
  when: ansible_distribution == 'Debian' and ansible_env.LANG != locale.locale

- include: ubuntu_locales.yml
  when: ansible_distribution == 'Ubuntu' and ansible_env.LANG != locale.locale
